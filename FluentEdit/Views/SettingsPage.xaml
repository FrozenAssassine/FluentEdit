<Page
    x:Class="FluentEdit.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:FluentEdit.Controls"
    mc:Ignorable="d"
    KeyDown="Page_KeyDown">

    <ScrollViewer x:Name="SettingsScrollViewer" VerticalScrollBarVisibility="Auto">
            
        <StackPanel Spacing="20" Margin="40,20,40,40" HorizontalAlignment="Stretch" Width="Auto">
            <!-- Seitentitel -->
            <TextBlock Text="Settings" 
                       Style="{StaticResource TitleTextBlockStyle}" 
                       Margin="0,0,0,10"/>

            <!-- SEKTION: ERSCHEINUNGSBILD -->
            <StackPanel Spacing="4">
                <TextBlock Text="Appearance" Margin="2,0,0,8"/>

                <!-- Font Einstellungen -->
                <Expander HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                    <Expander.Header>
                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <FontIcon Glyph="&#xE8D2;" FontSize="16"/>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Font Customization" Style="{StaticResource BodyTextBlockStyle}"/>
                                <TextBlock Text="Change family and size of the editor text" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
                            </StackPanel>
                        </Grid>
                    </Expander.Header>

                    <StackPanel Spacing="16" Margin="16">
                        <!-- Font Family -->
                        <Grid>
                            <TextBlock Text="Font Family" VerticalAlignment="Center"/>
                            <ComboBox x:Name="fontFamilyCombobox" 
                                      HorizontalAlignment="Right" Width="200"
                                      ItemsSource="{x:Bind Fonts}" 
                                      SelectionChanged="fontFamilyCombobox_SelectionChanged">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate x:DataType="x:String">
                                        <TextBlock FontFamily="{x:Bind}" Text="{x:Bind}" />
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                        </Grid>

                        <MenuFlyoutSeparator/>

                        <!-- Font Size -->
                        <Grid>
                            <TextBlock Text="Font Size" VerticalAlignment="Center"/>
                            <NumberBox x:Name="fontSizeNumberBox" 
                                       HorizontalAlignment="Right" Width="200"
                                       ValueChanged="fontSizeNumberBox_ValueChanged" 
                                       SpinButtonPlacementMode="Inline" 
                                       Maximum="48" Minimum="10" SmallChange="1"/>
                        </Grid>
                    </StackPanel>
                </Expander>

                <!-- Hintergrund & Design -->
                <Expander HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                    <Expander.Header>
                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <FontIcon Glyph="&#xE790;" FontSize="16"/>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Background &amp; Theme" Style="{StaticResource BodyTextBlockStyle}"/>
                                <TextBlock Text="Personalize your workspace materials and colors" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
                            </StackPanel>
                        </Grid>
                    </Expander.Header>

                    <StackPanel Spacing="16" Margin="16">
                        <!-- App Theme -->
                        <Grid>
                            <TextBlock Text="App Theme" VerticalAlignment="Center"/>
                            <ComboBox x:Name="themeCombobox" HorizontalAlignment="Right" Width="200" 
                                      SelectionChanged="themeCombobox_SelectionChanged">
                                <ComboBoxItem Content="System default" Tag="0"/>
                                <ComboBoxItem Content="Light" Tag="1"/>
                                <ComboBoxItem Content="Dark" Tag="2"/>
                            </ComboBox>
                        </Grid>

                        <MenuFlyoutSeparator/>

                        <!-- Backdrop Type -->
                        <Grid>
                            <TextBlock Text="Background Material" VerticalAlignment="Center"/>
                            <ComboBox x:Name="appBackgroundCombobox" HorizontalAlignment="Right" Width="200" 
                                      SelectionChanged="AppBackground_SelectionChanged">
                                <ComboBoxItem Content="Mica"/>
                                <ComboBoxItem Content="Acrylic"/>
                                <ComboBoxItem Content="Static"/>
                            </ComboBox>
                        </Grid>

                        <!-- Dynamische Controls je nach Background -->
                        <StackPanel x:Name="acrylicGrid" Spacing="16" Margin="0,8,0,0">
                            <Grid>
                                <TextBlock Text="Acrylic Tint Color" VerticalAlignment="Center"/>
                                <controls:ColorPickerButton x:Name="acrylicColorPicker" HorizontalAlignment="Right" 
                                                            IsAlphaEnabled="True" ColorChanged="acrylicColorPicker_ColorChanged"/>
                            </Grid>
                            <Grid>
                                <TextBlock Text="Persistent Acrylic" VerticalAlignment="Center"/>
                                <ToggleSwitch Style="{StaticResource LeftSideToggleSwitch}" x:Name="keepAcrylicOnFocusLost" HorizontalAlignment="Right" 
                                              OnContent="On" OffContent="Off"
                                              Toggled="keepAcrylicOnFocusLost_Toggled"/>
                            </Grid>
                        </StackPanel>

                        <Grid x:Name="staticGrid">
                            <TextBlock Text="Static Background Color" VerticalAlignment="Center"/>
                            <controls:ColorPickerButton x:Name="staticColorPicker" HorizontalAlignment="Right" 
                                                        IsAlphaEnabled="False" ColorChanged="staticColorPicker_ColorChanged"/>
                        </Grid>
                    </StackPanel>
                </Expander>
            </StackPanel>

            <!-- SEKTION: ALLGEMEIN -->
            <StackPanel Spacing="4">
                <TextBlock Text="General" Margin="2,0,0,8"/>
                <controls:SettingsControl IsTabStop="False" Header="Hide Donation Information" Glyph="&#xE170;">
                    <ToggleSwitch Style="{StaticResource LeftSideToggleSwitch}" Grid.Column="2" x:Name="hideDonationInfosButton" 
                                      Toggled="hideDonationInfosButton_Toggled" 
                                      OnContent="" OffContent="" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                </controls:SettingsControl>
            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</Page>